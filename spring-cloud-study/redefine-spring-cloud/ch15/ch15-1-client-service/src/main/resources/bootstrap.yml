server:
  port: 5566
spring:
  application:
    name: client-service
eureka:
  client:
    serviceUrl:
      defaultZone: http://${eureka.host:127.0.0.1}:${eureka.port:8761}/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true