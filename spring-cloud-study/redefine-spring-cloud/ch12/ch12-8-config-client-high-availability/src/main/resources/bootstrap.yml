spring:
    cloud:
        config:
            label: master
            uri: http://localhost:9090
            name: config-info
            profile: dev
            backup:
                enable: true
                fallbackLocation: /home/uj/temp/fallback.properties
